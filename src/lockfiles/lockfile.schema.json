{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "definitions": {
    "versionOrHash": {
      "description": "A version string or a VCS hash pin with source URL and optional fallback version.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "additionalProperties": false,
          "properties": {
            "rev": {
              "description": "VCS commit hash (git rev, etc.)",
              "type": "string"
            },
            "source": {
              "description": "VCS repository URL",
              "format": "uri",
              "type": "string"
            },
            "version": {
              "description": "Optional fallback version when rev is unavailable",
              "type": "string"
            }
          },
          "required": [
            "rev",
            "source"
          ],
          "type": "object"
        }
      ]
    }
  },
  "description": "Pinned versions for all package managers used across nucleus hosts.",
  "properties": {
    "bun": {
      "additionalProperties": {
        "$ref": "#/definitions/versionOrHash"
      },
      "description": "Bun-installed global package versions. Package name → version string or VCS hash pin.",
      "type": "object"
    },
    "cargo-binstall": {
      "additionalProperties": {
        "$ref": "#/definitions/versionOrHash"
      },
      "description": "cargo-binstall crate versions. Crate name → version string or VCS hash pin.",
      "type": "object"
    },
    "nixos-iso": {
      "additionalProperties": {
        "properties": {
          "sha256": {
            "type": "string",
            "description": "SHA-256 checksum of the NixOS minimal ISO for this architecture."
          },
          "url": {
            "type": "string",
            "format": "uri",
            "description": "URL to the NixOS minimal installation ISO for this architecture."
          }
        },
        "required": [
          "url",
          "sha256"
        ],
        "type": "object"
      },
      "description": "NixOS minimal ISO pins per architecture (x86_64-linux, aarch64-linux).  Updated by bump-lockfile.",
      "type": "object"
    },
    "ollama": {
      "additionalProperties": {
        "items": {
          "properties": {
            "digest": {
              "type": "string"
            },
            "name": {
              "type": "string"
            },
            "tag": {
              "type": "string"
            }
          },
          "required": [
            "name",
            "tag"
          ],
          "type": "object"
        },
        "type": "array"
      },
      "description": "Ollama model pins per host. Host name → array of model specs.",
      "type": "object"
    },
    "pwsh": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "PowerShell module versions. Module name → version string.",
      "type": "object"
    },
    "rustup": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Rustup toolchain versions. Channel name → date string.",
      "type": "object"
    },
    "scoop": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Scoop package versions. Package name → version string.",
      "type": "object"
    },
    "updated": {
      "description": "Last update timestamp in ISO-8601 format.",
      "format": "date-time",
      "type": "string"
    },
    "uv": {
      "additionalProperties": {
        "$ref": "#/definitions/versionOrHash"
      },
      "description": "uv (pip) tool versions. Tool name → version string or VCS hash pin.",
      "type": "object"
    },
    "version": {
      "description": "Lockfile schema version (monotonically increasing integer).",
      "minimum": 1,
      "type": "integer"
    },
    "vscode": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "VS Code extension versions. Extension ID → version string.",
      "type": "object"
    },
    "tart-images": {
      "additionalProperties": {
        "properties": {
          "digest": {
            "type": "string",
            "description": "OCI digest (sha256:...) of the Cirrus CI base image manifest."
          },
          "image": {
            "type": "string",
            "description": "OCI image reference (without digest)."
          }
        },
        "required": [
          "image",
          "digest"
        ],
        "type": "object"
      },
      "description": "macOS Tart base image OCI digests per macOS version (sequoia, tahoe, etc.).  Updated by bump-lockfile.",
      "type": "object"
    },
    "winget": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "WinGet package identifiers and versions. Package ID → version string. Lockfile is the sole version authority — only bump-lockfile may change WinGet versions.",
      "type": "object"
    },
    "homebrew": {
      "additionalProperties": false,
      "description": "Homebrew formula, cask, and Mac App Store version pins for macOS.",
      "properties": {
        "brews": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Homebrew formula versions. Formula name → version string.",
          "type": "object"
        },
        "casks": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Homebrew cask versions. Cask name → version string.",
          "type": "object"
        },
        "masApps": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Mac App Store app identifiers. App name → App Store ID string.",
          "type": "object"
        }
      },
      "required": [
        "brews",
        "casks",
        "masApps"
      ],
      "type": "object"
    }
  },
  "required": [
    "version",
    "updated",
    "scoop",
    "cargo-binstall",
    "bun",
    "uv",
    "rustup",
    "winget",
    "vscode",
    "pwsh",
    "homebrew",
    "ollama",
    "nixos-iso",
    "tart-images"
  ],
  "title": "Nucleus Lockfile",
  "type": "object"
}
