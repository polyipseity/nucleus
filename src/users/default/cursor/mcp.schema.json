{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Cursor global MCP server configuration (~/.cursor/mcp.json).",
  "properties": {
    "$schema": {
      "description": "Reference to the JSON Schema file that validates this data file.",
      "type": "string"
    },
    "mcpServers": {
      "additionalProperties": {
        "type": "object"
      },
      "description": "MCP servers available in all Cursor workspaces.",
      "type": "object"
    }
  },
  "required": [
    "mcpServers"
  ],
  "title": "Cursor MCP configuration",
  "type": "object"
}
