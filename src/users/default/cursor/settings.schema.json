{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Cursor IDE settings (Cursor User/settings.json).",
  "properties": {
    "$schema": {
      "description": "Reference to the JSON Schema file that validates this data file.",
      "type": "string"
    },
    "cursor.aiprovider.openai.apiKey": {
      "description": "API key for the OpenAI-compatible provider. The LiteLLM proxy is unauthenticated; this is a placeholder key.",
      "type": "string"
    },
    "cursor.aiprovider.openai.baseUrl": {
      "description": "Base URL of the OpenAI-compatible provider (LiteLLM proxy).",
      "type": "string"
    },
    "cursor.aiprovider.openai.model": {
      "description": "Model identifier for the OpenAI-compatible provider (LiteLLM model_name).",
      "type": "string"
    }
  },
  "title": "Cursor IDE settings",
  "type": "object"
}
