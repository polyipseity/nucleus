# Direnv base configuration (cross-platform).
# Host-specific overrides go in ~/.config/direnv/lib/*.sh — they are auto-sourced
# before this file by direnv's lib loading mechanism.
# See: src/users/default/direnv/lib/apple-sdk-override.sh (POSIX only)
