# System-level secrets shared across all hosts.
#
# This file is encrypted with SOPS.  The dummy placeholder below is replaced
# with the real value on each host via `sops edit src/secrets/system.yml`.
#
# Keys:
#   ai_openrouter_api_key — API key for OpenRouter AI gateway access.
ai_openrouter_api_key: dummy-placeholder-replace-with-real-key
