{
  "$id": "https://nucleus.local/key-catalog.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Catalog of AI API keys with env var mappings. Generated by apply scripts from decrypted system.yml; consumed by sops.nix and ai.nix at Nix evaluation time.",
  "properties": {
    "$schema": {
      "description": "Reference to this schema file.",
      "type": "string"
    },
    "keys": {
      "description": "List of AI API key entries. Each entry maps a SOPS secret name to the environment variable it should be exported as.",
      "items": {
        "additionalProperties": false,
        "properties": {
          "name": {
            "description": "SOPS secret name matching ai_*_api_key or ai_*_api_key_N.",
            "pattern": "^ai_.+_api_key(_[0-9]+)?$",
            "type": "string"
          },
          "envVar": {
            "description": "Environment variable name derived from the SOPS key name.",
            "pattern": "^[A-Z][A-Z0-9_]*$",
            "type": "string"
          }
        },
        "required": [
          "name",
          "envVar"
        ],
        "type": "object"
      },
      "type": "array",
      "uniqueItems": true
    }
  },
  "required": [
    "$schema",
    "keys"
  ],
  "type": "object"
}
