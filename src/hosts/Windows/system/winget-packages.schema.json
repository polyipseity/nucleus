{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "description": "Nix-generated list of WinGet package IDs enabled for the Windows host, derived from the shared overlap registry (overlappingPackages in core.nix). Consumed by apply.ps1 to filter packages.dsc.yml (Windows does not run Nix).",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "packages": {
      "description": "WinGet package identifiers that are enabled for the Windows host.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*(/[A-Za-z0-9][A-Za-z0-9._-]*)?$"
      },
      "uniqueItems": true
    }
  },
  "required": [
    "packages"
  ]
}
