# hosts/nixos/hardware/disks.nix — Disk-related hardware overrides placeholder.
#
# Keep this fragment present even when empty so future disk-specific settings
# have a stable import target and do not bloat unrelated hardware fragments.
{ ... }:
{
}
