--- a/libntfs-3g/Makefile.am	2026-07-08 14:10:56.263475694 -0700
+++ b/libntfs-3g/Makefile.am	2026-07-08 14:11:19.951341607 -0700
@@ -60,7 +60,10 @@
 install-exec-hook: install-rootlibLTLIBRARIES
 if INSTALL_LIBRARY
 	if [ ! "$(rootlibdir)" -ef "$(libdir)" ]; then \
-		$(MV) -f "$(DESTDIR)/$(libdir)"/libntfs-3g.so* "$(DESTDIR)/$(rootlibdir)";  \
+		for f in "$(DESTDIR)/$(libdir)"/libntfs-3g.*; do \
+			[ -e "$$f" ] || [ -L "$$f" ] || continue; \
+			$(MV) -f "$$f" "$(DESTDIR)/$(rootlibdir)"; \
+		done \
 	fi
 if GENERATE_LDSCRIPT
 	if [ ! "$(rootlibdir)" -ef "$(libdir)" ]; then \
