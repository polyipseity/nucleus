#!/usr/bin/env bash
# Logging-format policy fixture: allowlisted helper file name (lib.sh).
# Allowlisted files may contain raw ANSI escape literals.
# shellcheck disable=SC2034 # reason: fixture sample; variables are deliberately unused
RED='\033[0;31m'
NC='\033[0m'
