/*
 * Fixture macFUSE header for tests/scripts/macos-fuse-provider-tests.sh.
 * The provider digest hashes every regular file under include/fuse/, so only the
 * bytes of this file matter; it is not a compilable FUSE header.
 */
#define FIXTURE_FUSE_MAJOR 2
