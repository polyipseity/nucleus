---
# ref: https://json.schemastore.org/yamllint.json -- published schema (Spec G step 13)
$schema: https://json.schemastore.org/yamllint.json
extends: default

ignore: |
  src/modules/configs/discord-music-rpc/config.yaml
  src/secrets/
  vendor/**

rules:
  line-length: disable
